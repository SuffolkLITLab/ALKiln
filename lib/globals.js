let globals = {
  // File name prefix ALKiln can use to identify random answer generator files
  generators_prefix: `alkiln_random`,
  generated_files_folder_name: `_alkiln_generated`,
}

module.exports = globals;

// Discuss: Would this be clearer as a config file instead? If not, change name
// to `constants`?
